# Flux few-sample ablation: 16 cache files, grid10 (vs normal fast s64).
# sample_size stays -1 from model/flux.1-dev.yaml.
quant:
  calib:
    num_samples: 16
  smooth:
    proj:
      num_grids: 10

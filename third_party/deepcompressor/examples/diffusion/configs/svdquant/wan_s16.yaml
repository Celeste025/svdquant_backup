# Wan2.1-1.3B mid-scale PTQ: 64 cache files from 16-prompt collect (Flux-fast style).
# sample_size=-1 is set in model/wan2.1-1.3b.yaml (aligned with Flux); do not override to 32.
quant:
  calib:
    num_samples: 64
